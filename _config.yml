title: "Нотная библиотека"
description: "Церковные песнопения для обучения"
baseurl: ""
url: "https://voctrainer.github.io"

# Убираем тему minima, используем кастомные layout'ы
theme: null

plugins:
  - jekyll-relative-links

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: "partitures"
    values:
      layout: "folder"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "abc_partiture"
